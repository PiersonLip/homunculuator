FROM python:3.12-slim

ENV PYTHONUNBUFFERED=1

WORKDIR /app

RUN pip install --no-cache-dir \
    "py-cord>=2.7" \
    pyyaml \
    requests

# config.yaml is volume-mounted at runtime
COPY bot.py chat.py ./

CMD ["python", "bot.py"]
